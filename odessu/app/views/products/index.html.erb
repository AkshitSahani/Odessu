<h3 class="text-center">All Products</h3>
<div class="row">
  <div class="col-xs-6 col-xs-offset-3">
    <% @products.each do |product| %>
      <%= link_to product_path(product) do %>
        <div>
          <%= product.name %>
          <div><%= image_tag(product.picture_src, height: '300', width: '200') %></div>
          Price elsewhere: <span><%= product.pricebefore %></span><br>
          Price: <%= product.priceafter %><br><br>
        </div>
      <% end %>
    <% end %>
  </div>
</div>

<h1>Verify</h1>
<%= link_to 'Need help?', new_message_path(receiver_id: 1) %>
<% if current_user.id == 1 %>
  <%= link_to "my conversations", conversations_path %>
<% end %>
<%# User.calcAvrgFromStore(current_user) %>
